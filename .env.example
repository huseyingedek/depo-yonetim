# FRONTEND ortam değişkenleri ÖRNEĞİ.
# Kopyala:  cp .env.example .env
# ÖNEMLİ: Buraya CANIAS WSDL adresi veya şifre YAZILMAZ — onlar server/.env'dedir.
# Frontend sadece backend proxy'yi çağırır.

# Backend proxy adresi:
VITE_WMS_BASE_URL=http://localhost:8787/api/mzy
